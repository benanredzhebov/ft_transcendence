0. in login.ts I changed this part `const response = await fetch(`${import.meta.env.VITE_URL}/login`, {` to `const response = await fetch("/login", { `
- after this the another computer managed to login in

1. Email validation - check whether user is able to change his email
2. When is connected from another PC - Chad mode - ChatSocket for LiveChat not connected
3. When remote tournament finished appear back to dashboar button, not the correct bracket
4. 2FA - after filling out 6 digit code it's appear an error message "An error occurred during 2FA vertification"



5. When trying log in with google I noticed this issue : Access blocked: Authorization Error